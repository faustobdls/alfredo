# Open template format targets

date: 2026-09-02

Template format targets are project-defined identifiers, not a closed enum. The CLI may suggest known targets such as markdown, email, pptx, docx, or html, but custom pure-text, custom-file, renderer-specific, and future service targets are valid when they match the template id pattern.
